# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['bitcoin_dashboard_standalone.py'],
    pathex=[],
    binaries=[],
    datas=[('.env', '.')],
    hiddenimports=['sqlalchemy.sql.default_comparator', 'matplotlib', 'pandas', 'tkinter', 'PIL'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='BitcoinTracker',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['docs\\bitcoin.ico'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='BitcoinTracker',
)
